# 类型转换函数

```python
int("42")            # 转整数
float("3.14")        # 转浮点数
str(42)              # 转字符串
bool(0)              # 转布尔：False
list("abc")          # 转列表：['a', 'b', 'c']
tuple([1, 2, 3])     # 转元组：(1, 2, 3)
set([1, 2, 2, 3])    # 转集合：{1, 2, 3}
dict([("a", 1), ("b", 2)])  # 转字典
```