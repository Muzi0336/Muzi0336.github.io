# 输入输出函数

```python
# 输出
print("Hello")                   # 基本输出
print("a", "b", "c", sep="-")   # 自定义分隔符：a-b-c
print("Hello", end="!")          # 自定义结尾：Hello!
print(f"{num:.1f}")				#保留一位小数

# 输入
name = input("请输入名字：")     # 返回字符串
age = int(input("请输入年龄：")) # 需要类型转换
```